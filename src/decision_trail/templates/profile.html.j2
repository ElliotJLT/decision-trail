<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Collaboration Log</title>
  <meta name="description" content="Machine-captured collaboration patterns from real AI sessions. {{ profile.total_sessions }} sessions tracked.">

  <!-- Open Graph -->
  <meta property="og:title" content="Collaboration Log">
  <meta property="og:description" content="AI judgment has no feedback loop. {{ profile.total_sessions }} sessions of human-AI collaboration, machine-captured.">
  <meta property="og:type" content="profile">

  <!-- Twitter -->
  <meta name="twitter:card" content="summary">
  <meta name="twitter:title" content="Collaboration Log">
  <meta name="twitter:description" content="AI judgment has no feedback loop. {{ profile.total_sessions }} sessions of human-AI collaboration, machine-captured.">

  <style>
{{ css }}
  </style>
</head>
<body>
  <div class="container">

    <header class="hero">
      <h1>Collaboration Log</h1>
      <blockquote>AI fluency is measurable. AI judgment isn't. This log captures the difference.</blockquote>
      <p class="stats">
        <strong>{{ profile.total_sessions }} session{{ "s" if profile.total_sessions != 1 }}</strong>
        tracked since {{ profile.active_since }}
      </p>
    </header>

    <section>
      <h2>How I Work With AI</h2>
      <div class="how-i-work">
        {% if profile.how_i_work %}
        <p>{{ profile.how_i_work }}</p>
        {% else %}
        <p class="placeholder">Populates automatically after synthesis runs across enough sessions.</p>
        {% endif %}
      </div>
    </section>

    <section class="moments">
      <h2>Highlighted Moments</h2>
      {% if profile.highlighted_moments %}
      <ul>
        {% for moment in profile.highlighted_moments %}
        <li>{{ moment }}</li>
        {% endfor %}
      </ul>
      {% else %}
      <p class="placeholder">No sessions captured yet. Start a session to begin logging.</p>
      {% endif %}
    </section>

    {% if profile.digests %}
    <section>
      <h2>Activity</h2>
      <div class="timeline">
        {% for d in profile.digests %}
        <div class="day active" title="{{ d.date }}: {{ d.topic }}"></div>
        {% endfor %}
      </div>
    </section>
    {% endif %}

    <section>
      <h2>Evolution</h2>
      <div class="narrative">
        {% if profile.evolution_narrative %}
        <p>{{ profile.evolution_narrative }}</p>
        {% else %}
        <p class="placeholder">Needs more sessions. Evolution tracks how collaboration style changes over time.</p>
        {% endif %}
      </div>
    </section>

    <section class="moments">
      <h2>Beyond Fluency</h2>
      {% if profile.beyond_fluency_signals %}
      <ul>
        {% for signal in profile.beyond_fluency_signals %}
        <li>{{ signal }}</li>
        {% endfor %}
      </ul>
      {% else %}
      <p class="placeholder">Surfaces after synthesis — trust calibration, artifact paradox resistance, meta-cognition, strategic killing.</p>
      {% endif %}
    </section>

    <footer>
      Generated by <a href="https://github.com/ElliotJLT/decision-trail">decision-trail</a>.
      Not self-reported. Machine-captured from real Claude Code sessions.
    </footer>

  </div>
</body>
</html>
