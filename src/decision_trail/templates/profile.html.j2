<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Decision Profile</title>
  <meta name="description" content="How I think when building with AI. {{ profile.total_sessions }} sessions tracked.">

  <!-- Open Graph -->
  <meta property="og:title" content="Decision Profile">
  <meta property="og:description" content="Coding is solved. Thinking isn't. {{ profile.total_sessions }} sessions of human-AI collaboration, machine-captured.">
  <meta property="og:type" content="profile">

  <!-- Twitter -->
  <meta name="twitter:card" content="summary">
  <meta name="twitter:title" content="Decision Profile">
  <meta name="twitter:description" content="Coding is solved. Thinking isn't. {{ profile.total_sessions }} sessions of human-AI collaboration, machine-captured.">

  <style>
{{ css }}
  </style>
</head>
<body>
  <div class="container">

    <header class="hero">
      <h1>Decision Profile</h1>
      <blockquote>Coding is solved. Thinking isn't. This is how I think.</blockquote>
      <p class="stats">
        <strong>{{ profile.total_sessions }} session{{ "s" if profile.total_sessions != 1 }}</strong>
        tracked since {{ profile.active_since }}
      </p>
    </header>

    <section>
      <h2>How I Work With AI</h2>
      <div class="how-i-work">
        {% if profile.how_i_work %}
        <p>{{ profile.how_i_work }}</p>
        {% else %}
        <p class="placeholder">Run /trail to generate a synthesis — this section populates from recurring patterns across sessions.</p>
        {% endif %}
      </div>
    </section>

    <section class="moments">
      <h2>Highlighted Moments</h2>
      {% if profile.highlighted_moments %}
      <ul>
        {% for moment in profile.highlighted_moments %}
        <li>{{ moment }}</li>
        {% endfor %}
      </ul>
      {% else %}
      <p class="placeholder">No digests yet. Run /marmite in a Claude Code session to start capturing.</p>
      {% endif %}
    </section>

    {% if profile.digests %}
    <section>
      <h2>Activity</h2>
      <div class="timeline">
        {% for d in profile.digests %}
        <div class="day active" title="{{ d.date }}: {{ d.topic }}"></div>
        {% endfor %}
      </div>
    </section>
    {% endif %}

    <section>
      <h2>Evolution</h2>
      <div class="narrative">
        {% if profile.evolution_narrative %}
        <p>{{ profile.evolution_narrative }}</p>
        {% else %}
        <p class="placeholder">Needs more sessions. Evolution tracks how collaboration style changes over time.</p>
        {% endif %}
      </div>
    </section>

    <section>
      <h2>Beyond Fluency</h2>
      <div class="narrative">
        {% if profile.beyond_fluency_evidence %}
        <p>{{ profile.beyond_fluency_evidence }}</p>
        {% else %}
        <p class="placeholder">Run /trail to surface evidence of trust calibration, artifact paradox resistance, meta-cognition, and strategic killing.</p>
        {% endif %}
      </div>
    </section>

    <footer>
      Generated by <a href="https://github.com/ElliotJLT/decision-trail">decision-trail</a>.
      Not self-reported. Machine-captured from real Claude Code sessions.
    </footer>

  </div>
</body>
</html>
